<html lang="en">
<% include ../partials/head %>

  <body class = "container">
      <!-- Write EJS code here to display shortened links -->
    <header>
      <% include ../partials/header %>
    </header>

    <main>
      <div style="padding:1px 200px;height:1000px;background-color:#8C7AAE">ron">
        <h1>Url dataBase</h1>
        <ul>
          <% for(index in urls) { %>
            <li>ID: <%= index%> </li>
            <p>URL: <%= urls[index] %></p>
          <% }; %>
        </ul>
      </div>
    </main>
  </body>
</html>